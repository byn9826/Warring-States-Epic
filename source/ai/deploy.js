Vue.mixin({
    methods: {
        AIdeployMarchTargets: function(own, state) {
            var targets = [];
            state[own].nearby.forEach(function(nearby) {
                
            });
        }
    }
});