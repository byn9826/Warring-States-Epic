import { StyleSheet } from 'react-native';

const commonStyle = StyleSheet.create({
  stateName: {
    color: 'white',
    padding: 5,
    fontSize: 16,
    fontWeight: 'bold',
  },
});

export default commonStyle;
