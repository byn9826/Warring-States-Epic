import { StyleSheet } from 'react-native';

const commonStyle = StyleSheet.create({
  stateName: {
    color: 'white',
    padding: 5,
    fontSize: 16,
    width: 26,
    height: 26,
    fontWeight: 'bold',
  },
});

export default commonStyle;
